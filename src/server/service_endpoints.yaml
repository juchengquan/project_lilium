# TODO: may need mode templated for diff models (e.g., HF, CT2)

text_generation:
- path: /infer
  endpoint: api_inference
  method: 
  - POST
- path: /infer_stream
  endpoint: api_inference_stream
  method: 
  - POST

embedding:
- path: /embed
  endpoint: api_embedding
  method: 
  - POST

dummy:
- path: /infer
  endpoint: api_inference
  method:
  - POST